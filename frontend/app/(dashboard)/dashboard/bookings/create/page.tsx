import BookingForm from "../_components/form/BookingForm";

export const metadata = {
  title:"Booking Create"
}

export default function BookingCreate() {
    return (
        <BookingForm/>
    );
}